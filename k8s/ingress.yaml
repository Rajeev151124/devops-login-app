apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: login-ingress
spec:
  rules:
    - host: login.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 9091

